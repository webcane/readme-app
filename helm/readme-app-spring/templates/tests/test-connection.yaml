apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "readme-app-spring.fullname" . }}-test-connection"
  labels:
    {{- include "readme-app-spring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "readme-app-spring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
