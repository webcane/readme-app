apiVersion: v1
kind: Service
metadata:
  name: {{ include "readme-app-spring.fullname" . }}
  labels:
    {{- include "readme-app-spring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "readme-app-spring.selectorLabels" . | nindent 4 }}
