spring:
  security:
    basic:
      enabled: false
    #    user:
    #      name: cane

    oauth2:
      client:
        registration:
          github:
            clientId: ${client.id}
            clientSecret: ${client.secret}